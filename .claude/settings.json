{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Autoresearch context-integrity gate (THS autoresearch harness). The repo had no settings.json before this install, so this file IS the merge of .claude/settings.snippet.json. ci_gate.py = PreToolUse verify-before-assert gate on Write/Edit/MultiEdit/Bash; ci_record.py = PostToolUse verification logger on Read/Grep/Glob/Bash; ci_session_init.sh = per-session log init. All fail-open. See .claude/rules/context-integrity.md + AUTORESEARCH_DOCTRINE.md.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|Bash",
        "hooks": [
          { "type": "command", "command": "python3 ${CLAUDE_PROJECT_DIR}/.claude/hooks/ci_gate.py", "timeout": 5 }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Read|Grep|Glob|Bash",
        "hooks": [
          { "type": "command", "command": "python3 ${CLAUDE_PROJECT_DIR}/.claude/hooks/ci_record.py", "timeout": 5 }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/ci_session_init.sh", "timeout": 5 }
        ]
      }
    ]
  }
}
