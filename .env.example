# Edge Function secrets. Copy to .env.local and fill in for local dev.
# Production values live in Supabase Edge Function secrets (Dashboard) and
# the Postgres vault (see supabase/migrations/*_cron.sql).

FMP_API_KEY=
CRON_INVOKE_SECRET=
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=
