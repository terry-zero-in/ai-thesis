# HP-1 Handoff — 2026-06-11

## §1 State snapshot

- **Product:** HP-1, Terry's new AI-equity trading system (separate from the AI Thesis portal product). Strategy/spec phase — no repo, no Linear project, no deployed code yet.
- **Phase status:** Phase 1 (algorithm + backtest) DONE · head-to-head vs AI Thesis v2 DONE, merge ratified · Phase 2 (Fable review rubric) DONE · Phase 3 (UI spec) NOT STARTED · ANTH diligence NOT STARTED.
- **Receipts:** backtest self-test PASS; 50/50 universe names loaded, 0 missing, data through 2026-06-10 (yfinance, dividend-adjusted). 24m blend: 110.5% CAGR / 2.43 Sharpe / −33.7% MaxDD vs EW-50 79.3%/1.93 and QQQ 23.0%/0.88; 36m ordering holds (full tables in `results_24m.csv` + spec). Engine reference implementation exported: `hp1_backtest.py`.
- **Basis:** N/A this session. **AI Thesis portal:** no tickets touched; one scope-relevant decision — v2 algorithm retired as a standalone *for the HP-1 account only*; portal may keep it for its original long-horizon purpose (Terry's call, §4).

## §2 This session's delta — locked, do not relitigate

1. **HP-1 engine locked** (spec §3): Tactical = 0.45·zM + 0.35·zRAM + 0.20·zDD on 100d-MA gate; Core = 12m-tilted price composite ×0.75 + fundamental overlay ×0.25 on 200d gate. Rationale: risk-managed momentum is the replicated literature (Barroso/Santa-Clara, Daniel/Moskowitz, Faber) and measured +31–42 CAGR pts / +0.5–0.7 Sharpe vs equal-weighting the same 50 names over both windows, costs in.
2. **Momentum weights stand** against AI Thesis v2's downweight — v2's SSRN citation covers long-short momentum across crash regimes; the fix is risk management, not elimination. Evidence in `HP1_vs_AIThesis_v2_verdict.md`.
3. **Six merge deltas ratified** ("Go ahead and write," 2026-06-11): L1–L5 layers (L3 split a/b for cluster caps) · v2 Tier-A Q/G/V machinery incl. depreciation/Burry penalty becomes the Core overlay at capped 25% · v2 S-signals + NAAIM/AAII/F&G become Fable downgrade-only flags · v2 macro multiplier dropped (inert) · momentum unchanged · v2 standalone retired for this account.
4. **Fable rubric locked** (`FABLE_REVIEW_RUBRIC_v1.md`): bounded asymmetric rescoring [−20,+5] on engine percentile · CONFIRM-with-no-findings is the expected default · no citation → no effect · Fable accelerates mechanical exits, never delays · review set ~20–28 names · post-close every 2 trading days + event triggers · JSON schemas (rubric §7/§8) are the UI data contract · verbatim system prompt in rubric §10.
5. **ANTH path locked** (spec §7): Anthropic has FILED for IPO (headline fetch 2026-06-11 — Reuters/CNBC/TechCrunch; ~$1T press chatter). Path = conditional brokerage IPO allocation + pre-committed valuation ceiling (multiple of verified run-rate) + three tranche windows (allocation/day-1, post-first-earnings, lockup ~180d); pre-IPO secondary only if at/below last private round. Rubric ANTH block returns WAIT until the ceiling is set.
6. **Universe locked at 50 + ANTH** (spec §2 table) — Terry never redlined; treat as standing unless he swaps at quarterly review.

## §3 Gotchas

- **Stooq is blocked** from container IPs (JS proof-of-work wall). Use yfinance (`auto_adjust=True` = dividend-adjusted).
- **The zM bug pattern:** pandas `.where()` against a concat with mismatched column labels silently produced `inf` z-scores and degenerate ranks. Fixed with numpy weighted-mean + `isfinite` assertion in `hp1_backtest.py`. Keep the assertion in any reimplementation.
- **The May14→Jun10 check is NOT proof:** true out-of-sample for v2, in-sample for HP-1, 19 days of noise. Quote only with the asymmetry note (verdict doc has the exact wording).
- **Backtest caveats are mandatory in any restatement:** universe survivorship (engine sorts within a universe picked knowing outcomes — EW-50 is the honest baseline), single regime, pre-tax, 15 bps/side. Never project the absolute CAGR; project the relative edge.
- **Never backtest the fundamental overlay with current fundamentals** — lookahead. It stays live-only at capped weight until `scores_history` accumulates point-in-time data (12+ months).
- In the AI Thesis v2 source doc, per-dimension AIQ sums are authoritative over composite text (the doc itself notes arithmetic drift in two names).
- Fable layer never recomputes engine math — engine numbers arrive as inputs (rubric §3).

## §4 Open decisions for Terry — exhaustive

1. **ANTH ceiling multiple** — after the diligence session brings a recommended number with comp evidence. Money = Terry's column.
2. **Capital split/timing** of the $300–500K across sleeves + ANTH carve-out — untouched by any spec; Terry's column.
3. **App placement:** HP-1 UI as a new surface inside the AI Thesis portal vs a standalone app; repo name; whether to open a Linear project (new prefix?).
4. **Design tokens:** default per precedent ("match Basis on all") = the locked Basis v1 system. Needs explicit ratification in the UI session, with the live token file in hand.
5. **AIQ seeds:** port v2's 20 hand-scored names, Fable-draft the remaining 30 for Terry's ratification; refresh cadence Aug 2026.
6. **Gauge scraper** (NAAIM/AAII/F&G): optional build — gauges are flags, not mechanics.
7. **AI Thesis portal fate:** keep running v2 for its long-horizon purpose, or fold entirely into HP-1.

## §5 Verify before trusting

- All seven files in §6 exist in outputs (`ls` receipt this session). `verify-don't-assume`: re-confirm on attach.
- `current_ranks.csv` is as-of **2026-06-10** — STALE for any live decision; rerun the engine (`hp1_backtest.py`) before quoting ranks in a new session.
- Anthropic IPO status = headline-level fetch only (RSS titles, 2026-06-11). The ANTH session re-verifies filing terms/timing/range from primary sources before anything else.
- Basis token values were deliberately NOT restated anywhere — the live token file is canonical; the UI session reads the actual file Terry attaches.

## §6 Files & re-attach list

Produced this session (all in outputs): `HP1_SPEC_v1.1.md` (canonical spec) · `FABLE_REVIEW_RUBRIC_v1.md` (canonical rubric + UI data contract) · `HP1_vs_AIThesis_v2_verdict.md` (merge rationale, history) · `HP1_SPEC_v1.md` (superseded, history) · `results_24m.csv` (canonical backtest record) · `current_ranks.csv` (ranks as-of 6/10, stale-able) · `hp1_backtest.py` (engine reference implementation). Source doc referenced: `AI-Thesis-v2-Algorithm-and-Deployment.md` (Terry's upload).

**Attach to UI session:** this handoff + `HP1_SPEC_v1.1.md` + `FABLE_REVIEW_RUBRIC_v1.md` + the locked Basis v1 token file (Terry attaches from his repo).
**Attach to ANTH session:** this handoff + `HP1_SPEC_v1.1.md`.

## §7 First tasks

**UI session:**
1. Load `/craft`; read the attached Basis v1 token file; confirm token inheritance with Terry (§4.4) before any pixel decision.
2. Write `HP1_UI_SPEC_v1.md` — surfaces: **Decision Sheet** (leads with `decisions[]`, max 5 ranked lines from rubric §8), **Rank Board** (Combined / Pure-play / Megacap toggle per spec §2; columns: rank, Δ vs last run, driver tag, engine pct vs adjusted pct, verdict chip, next_event), **Holdings strip** (sleeve, exit-trigger proximity, migration flags), **ANTH tile** (GO/WAIT/STOP + vs_ceiling), **Regime strip** (breadth, gate gross, gauges). Data contract = rubric §7/§8 JSON verbatim. Anti-requirements: no memos, no decorative charts, compact-first.
3. Render the Decision Sheet first — template-first; it sets the pattern the other surfaces inherit.

**ANTH session:**
4. Pin Anthropic revenue run-rate to primary-source reporting (test Terry's claims: 12–13x YoY, 5x since 1/1/26, vs ~$9B exit-2025 base ⇒ ~$40–45B implied) and the actual filing terms/timing/range.
5. Build the comp table (public AI/hyperscaler EV-to-fwd-revenue vs growth, OpenAI marks, recent hypergrowth IPO pricings) → deliver a recommended ceiling multiple with evidence → Terry ratifies (§4.1).

## §8 Do NOT

Relitigate the six deltas, momentum weights, or the 50-name universe absent a Terry redline · quote the 19-day check as proof · backtest the overlay on current fundamentals · restate design tokens from memory · let the Fable layer recompute engine math · revive AI Thesis v2 standalone for this account · expand the UI beyond the five surfaces in §7.2 without Terry.
